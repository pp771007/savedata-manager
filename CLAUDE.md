# 專案規則

## 版號

- **每次 push 前都要更新版號**，不可沿用上一版。
- 版號定義在 `tool.js` 最上方的 `var VERSION = 'x.y.z';`（會顯示在工具面板標題的 `v` 後面）。
- 採語意化版號（SemVer）：
  - 修 bug / 微調文案、樣式 → 第三碼 +1（patch）
  - 新功能、不破壞既有用法 → 第二碼 +1、第三碼歸零（minor）
  - 破壞性變更（資料格式、書籤介面不相容）→ 第一碼 +1，其餘歸零（major）
- 改完版號再 commit、push；建議在 commit message 帶上新版號方便追蹤。

## 專案結構

- `tool.js`：書籤注入目標網頁的主程式（備份 / 還原 / 備份至此 / 匯出 / 匯入），以 Shadow DOM 隔離 UI。
- `index.html`：安裝教學頁（GitHub Pages），動態產生書籤程式碼指向同站的 `tool.js`。
- 純前端、無建置步驟、不上傳任何資料；備份存在使用者瀏覽器的 IndexedDB。
